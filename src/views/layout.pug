doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Sistema Obras
    style.
      a:link, 
      a:visited {
        color: blue;
        text-decoration: none; 
      }
            
      a:hover {
        color: darkblue;
        text-decoration: underline;
      }
  body
    header
      h1 Cimientos Sólidos S.A. - TechArg 
        nav
          ul
            li
              a(href='/') Inicio

    main
      block content 

    footer
      p &copy; #{new Date().getFullYear()} Sistema Node.js con JSON - TechArg